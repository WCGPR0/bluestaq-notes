spring:
  application:
    name: notes-api-test

de:
  flapdoodle:
    mongodb:
      embedded:
        version: 7.0.4

app:
  jwt:
    secret: test-only-secret-do-not-use-in-production-0123456789abcdef
    expiration-seconds: 3600
